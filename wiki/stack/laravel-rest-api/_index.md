---
title: Laravel REST API (corpus)
description: The sharded reference corpus behind the fleet-api-specification — HTTP semantics from the RFCs, the Laravel output/input primitives, spatie packages, versioning, problem details, auth, and the OpenAPI contract chain. One concept per page; the normative rules live in the spec.
tags: [stack, index, api, rest, laravel, http]
type: stack
status: reference
updated: 2026-07-30
related: [fleet-api-specification, laravel-architecture-manual]
---

# Laravel REST API — the reference corpus

Deep reference behind **[[fleet-api-specification]]** (the rule of record — read that first;
these pages carry the evidence, mechanics, and exact citations). Researched 2026-07-30 from
primary sources: RFC 9110/9111/9457/6585/8288/9745/8594 + IETF drafts, Laravel docs and
framework source, spatie docs, and the Stripe/GitHub/Shopify/Zalando/Azure/Google guidelines.

<!-- shards:begin -->
<!-- generated by `bin/wiki index` — do not hand-edit between the markers -->
- [[api-filtering-sorting|Filtering, Sorting, Includes — the Query Vocabulary]] — The spatie/laravel-query-builder v7 conventions the fleet adopts — filter[x], sort=-field, include=, fields[type] — deny-by-default…
- [[api-pagination|API Pagination — Offset, Cursor, and the Guardrails]] — paginate vs simplePaginate vs cursorPaginate — exact envelopes, when cursor pagination is mandatory territory, the per_page cap,…
- [[api-rate-limiting|API Rate Limiting — the Limiter, the Headers, the 429]] — The mandatory RateLimiter::for('api') definition and throttle:api wiring, Laravel's actual header emissions verified from source,…
- [[api-resources|Eloquent API Resources — the Output Layer]] — JsonResource/ResourceCollection as the response contract — wrapping facts (why unwrapping guarantees inconsistency), the full…
- [[api-versioning|API Versioning — Path Majors, Additive Evolution, Sunset]] — Why the fleet versions in the URL path with integer majors, the breaking-vs-additive taxonomy that makes versioning cheap, prior art…
- [[conditional-requests-etags|Conditional Requests, ETags & API Caching]] — The lost-update protection pattern — strong ETags, If-Match on writes, 428 for missing preconditions, 412 for failed ones, 304 revalidation…
- [[content-negotiation|Content Negotiation — JSON-Only, Forced, and Vary-Correct]] — The fleet's one-representation policy — Accept handling and the 406 ruling, the ForceJsonResponse middleware + shouldRenderJsonWhen pair…
- [[http-method-semantics|HTTP Method Semantics (RFC 9110 §9, RFC 5789)]] — The method properties table — safety, idempotency, cacheability — plus the exact PUT/POST/PATCH/DELETE rules APIs get wrong, with RFC…
- [[http-status-codes|HTTP Status Codes for APIs (RFC 9110 §15, RFC 6585)]] — The CRUD-relevant status registry with companion-header obligations — what each code means, which headers are RFC-MUSTs (Allow on 405,…
- [[idempotency-keys|Idempotency-Key — Safe Retries for Unsafe Writes]] — The IETF draft semantics the fleet adopts for retry-safe POSTs — replay returns the stored result, same-key/different-payload → 422,…
- [[openapi-scramble|OpenAPI & the Contract Chain — Scramble, oasdiff, Spectator]] — The four-link enforcement chain that makes the API contract undriftable — Scramble generates OpenAPI 3.1 from FormRequests/Resources, the…
- [[problem-details|Problem Details — RFC 9457 in Laravel]] — The application/problem+json error contract — members, type URIs, the spec's own 422 validation shape with JSON Pointers, and how the fleet…
- [[rest-maturity-and-doctrine|REST Maturity & the Fleet Doctrine]] — What REST formally requires (Fielding, HATEOAS) vs what shipping APIs actually do (Richardson Level 2), and the honest position the fleet…
- [[sanctum-token-auth|Sanctum Token Auth — Abilities, the Clamp, and the Traps]] — Personal access tokens as the fleet API's only credential — resource:verb ability naming, one ability per route via the ALL-of middleware…
- [[spatie-laravel-data|spatie/laravel-data — the Typed Carrier (and Its Limits)]] — Where laravel-data fits the fleet API (typed DTO between FormRequest and domain, TypeScript generation) and where it is deliberately not…
<!-- shards:end -->
