---
title: Standards
description: The fleet engineering standard — the rule of record (fleet-app-specification), the philosophy + map (laravel-engineering-standard), and the define-and-measure auditor (cquality). The runnable enforcement artifacts live in the repo at standards/laravel/; the dated rollout is a convergence log.
tags: [meta, index, standard]
type: standard
status: reference
updated: 2026-06-27
related: [fleet-app-specification, laravel-engineering-standard, cquality]
---

# Standards

How every fleet Laravel app is engineered the same way. Three pages, three roles:

- **The rule** → [[fleet-app-specification]] — the normative, mandated value for every
  operational concern. The requirement of record.
- **The philosophy + map** → [[laravel-engineering-standard]] — why, the two halves, the
  audit→converge→re-audit loop.
- **Define & measure** → [[cquality]] — the audit rubric (public OSS).

The **runnable enforcement artifacts** apps copy from live in the repo at
`standards/laravel/` (configs, CI template, arch suite, scaffold). The **dated rollout
history** is a convergence log; the **open burndown** is a convergence backlog.

<!-- shards:begin -->
<!-- generated by `bin/wiki index` — do not hand-edit between the markers -->
- [[audit-logging-standard|Audit / Activity-Logging Standard (v1 — capture, dashboard, partitioned append-only storage)]] — The normative rule set for the entity audit trail on every fleet app — owen-it/laravel-auditing for old→new diffs with a resolved causer,…
- [[backup-dr-standard|Backup, DR & PITR Standard (v1 — what is backed up, RPO/RTO tiers, restore drills, the DR runbook)]] — The normative rule set for durability and recovery on every fleet app — what MUST be backed up versus what GitOps re-creates, Postgres base…
- [[broadcasting-realtime-guidance|Real-Time & Broadcasting Guidance (Reverb)]] — Best-practice guidance for apps that adopt realtime — when a websocket is warranted at all, Laravel Reverb's config and k8s footprint (own…
- [[cquality|cquality — Laravel code-quality audit framework]] — The fleet's canonical code-quality auditor
- [[datetime-timezone-doctrine|Date, Time & Timezone Doctrine (v1 — storing, computing, scheduling, rendering)]] — The normative rule set for every datetime value in a fleet app — Postgres `timestamptz` on every column behind a Blueprint macro with a…
- [[encryption-at-rest-doctrine|Encryption-at-Rest Doctrine (v1 — two layers, crown jewels, key custody)]] — The normative rule set for data at rest on every fleet app — the two sequenced layers (infrastructure volume/bucket/snapshot encryption as…
- [[feature-flags-standard|Feature Flags Standard (v1 — Pennant classes, scope, exposure, retirement)]] — The normative rule set for feature flags on every fleet Laravel app — Laravel Pennant with one class-based Feature per capability so the…
- [[file-storage-standard|File Storage & Uploads Standard (v1 — disks, keys, ingest, egress, lifecycle)]] — The normative rule set for user files in every fleet Laravel app — the Storage facade over an S3-compatible backend as the only sanctioned…
- [[fleet-api-specification|Fleet API Specification (v1 — the REST API rule of record)]] — The normative Specification for every HTTP API a fleet app exposes — architecture, URLs, requests, responses, errors, versioning, auth,…
- [[fleet-app-specification|Fleet App Specification (v1 — mandated operational config)]] — The normative Specification that mandates how every fleet Laravel app is configured and run — runtime/framework versions, static & lint…
- [[fleet-frontend-specification|Fleet Front-End Specification (v1 — mandated front-end mechanics)]] — The normative Specification for how every fleet Inertia+React app is built on the front end — the mechanical/expressive split,…
- [[fleet-local-gate|The local gate (run this before you push)]] — The one owner of what "gated" means locally on a fleet Laravel app — the full static set, the two things composer ci:check does NOT run,…
- [[fleet-queue-doctrine|Fleet Queue Doctrine (v1 — partitioning, coding, operating, observing)]] — The normative rule set for background work on every fleet Laravel app — how queues are defined and partitioned (and when to shard), how job…
- [[fleet-testing-doctrine|Fleet Testing Doctrine (v1 — what to test, what not to test)]] — The normative judgment layer for testing every fleet Laravel app — the unit-first law (bootless units over an extracted pure core), the…
- [[fleet-webhook-specification|Fleet Webhook Specification (v1 — the outbound webhook rule of record)]] — The requirement of record for outbound webhooks in fleet Laravel apps — event taxonomy and catalog, domain model and outbox persistence,…
- [[framework-bump-playbook|Framework Bump Playbook (annual PHP/Laravel rituals + feature-rulings register)]] — The standing procedure that keeps the standards current instead of re-auditing them by hand — the Q4 PHP bump ritual, the Q1 Laravel bump…
- [[inbound-webhooks-pattern|Inbound Webhook & Integration Pattern (guidance — receiving third-party events)]] — The reusable pattern for the other direction — a fleet app receiving webhooks from a provider it does not control
- [[laravel-engineering-standard|Laravel Engineering Standard (philosophy + index)]] — The philosophy behind managing every fleet Laravel app the same way — the two halves (cquality defines & measures, this repo enforces &…
- [[money-currency-doctrine|Money & Currency Doctrine (v1 — minor-unit integers, brick/money, boundary adapters)]] — The normative rule set for every monetary amount in a fleet Laravel app — the float ban, the `*_minor` BIGINT + `currency_id` FK column…
- [[notifications-standard|Notifications Standard (v1 — channels, the in-app model, preferences, providers)]] — The normative rule set for notifying a *person* in every fleet Laravel app — the four-channel set (database, broadcast, mail, SMS) and the…
- [[observability-slo-standard|Observability, Metrics & SLO Standard (v1 — the metrics plane, the golden set, SLOs & error budgets)]] — The normative rule set for the metrics/tracing/SLO layer that sits ABOVE the fleet's baseline telemetry — Prometheus exposition from a…
- [[php-language-doctrine|PHP Language Doctrine (8.3–8.5 rulings, 8.6 forward guard)]] — The normative ruling set for modern PHP language features in fleet code — which 8.3/8.4/8.5 idioms are mandated, preferred, deferred, or…
- [[security-governance|Security Governance — green-then-ratchet]] — How security and quality findings are operated rather than merely discovered — the four-beat adoption discipline that makes a gate stick,…
- [[tenant-isolation-rls|Tenant Data Isolation & Postgres RLS (v1 — database-per-tenant, row security, the session principal)]] — The normative rule set for who may read a row on every fleet Laravel app — database-per-tenant behind a per-tenant Postgres role as the…
- [[testing-antipattern-catalog|Testing Anti-Pattern Catalog (smells, harms, fixes, detection)]] — The exhaustive catalog behind [[fleet-testing-doctrine]] — every test smell the fleet bans or watches, in six families (framework-testing,…
- [[uptime-alerting-standard|Outside-In Uptime Alerting Standard (v1 — external probing, check sets, alert routing, coverage)]] — The normative rule set for knowing a fleet app has stopped serving — why detection MUST be outside-in (the prober exercises DNS → TLS → CDN…
<!-- shards:end -->
